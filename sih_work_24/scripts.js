// Sample product data (replace with your actual data)
const products = [
  { 
      name: "Pumpkin Seeds",
      image: "images/pumpkin-seeds.jpg",
      price: 10.00,
      category: "Marrow" 
  },
  // ... add more product data ...
];

const productGrid = document.querySelector(".product-grid");

// Loop through products and create HTML for each product
products.forEach(product => {
  // Create a div for the product item
  // Set image, name, price, etc. using product data
  // Append the product item to the productGrid
}); 